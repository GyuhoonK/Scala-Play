object Ex_5_2{
    def main(args:Array[String]) = {
        val result = sum(1,2)
        println(result)
    }
    // def sum(a:Int, b:Int):Int = {
    //     a + b
    // }
    def sum(a:Int, b:Int):Int = a+b // 짧게 줄여서 쓸 수 있다 !!!
}